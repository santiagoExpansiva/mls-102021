{
  "flowId": "agentChangeBackend",
  "schemaVersion": "2026-06-26-v1-autonomous-create",
  "description": "Stage 3 backend reconciler for projects generated by agentNewSolution2. V1 is AUTONOMOUS and CREATE-ONLY: with no user prompt, it scans l4 for owners with statusBackend = toCreate, derives the 3-layer HEXAGONAL backend, generates the .defs.ts and materializes them to .ts, then marks the owners done. If there is nothing to create, it finishes immediately without writing files or statuses. Independent of statusFrontend (agentChangeFrontend). See spec.md in this folder.",
  "entry": {
    "args": {},
    "mode": "autonomous_scan",
    "noInputPolicy": "No user prompt. The agent receives no module/owner/item arguments; it discovers all actionable work from l4 statusBackend. (A single-item arg is an optional future convenience, not required.)",
    "noWorkBehavior": "If no owner with statusBackend = toCreate is found, finish without changing any file or status.",
    "statusProcessedInV1": [
      "toCreate"
    ]
  },
  "source": {
    "spec": "_102021_/l2/agentChangeBackend/spec.md",
    "stage1Flow": "_102020_/l2/agentNewSolution2/flow.json",
    "siblingFlow": "_102020_/l2/agentChangeFrontend/flow.json (same autonomous create-only pattern, statusFrontend side)",
    "tree": "_102021_/l2/agentChangeBackend/agentChangeBackend.ts + per-agent enqueueNext/dispatch (IMPERATIVE sequential chain; NOT a declarative planned tree). Two parallel_dynamic fan-outs: cb-usecase-fanout and cb-materialize (one layer per dispatch).",
    "schemas": "_102021_/l2/agentChangeBackend/cbSchemas.ts (strict JSON schemas; collab-llm enforces the tool call, cbExtract re-validates) — TO BUILD",
    "reused": "_102021_/l2/agentMaterializeSolution/* (agentPrepareDefsL1, agentMaterializeL1Def, agentMaterializeGen, templateMaterialize, registerBackEnd)"
  },
  "scope": {
    "v1": [
      "Run autonomously with no user prompt; discover work from l4 statusBackend.",
      "Process ONLY owners with statusBackend = toCreate (new). Exit cleanly when there is no create work.",
      "Derive aggregates from ontology kind + relationships; apply the JSONB model (indexed columns out, the rest + child collections in details).",
      "Generate the 3-layer hexagonal .defs.ts (domain, application, adapters) and materialize them to .ts via the reused L1 pipeline.",
      "Register the persistence manifest, the http router and the port->adapter composition root; fix l0/config.json backend entrypoints.",
      "Mark processed owners statusBackend = done after their artifacts + .ts validate.",
      "Use JSON-schema-backed tool output whenever an LLM is used (agentNewSolution2 pattern)."
    ],
    "explicitlyNotInV1": [
      "No user-provided module/owner/item filters (pure autonomous scan).",
      "No clarification / no blocking human input.",
      "No processing of toUpdate / toRemove / inProgress / done owners."
    ],
    "deferredToV2": [
      "toUpdate diff and preservation of manual edits.",
      "toRemove cascade (deprecate table/impl, drop with confirmation, data safety).",
      "Patching artifacts created by a previous run.",
      "Critic/repair LLM loop for referential integrity beyond the deterministic checks."
    ],
    "notResponsible": [
      "MDM tables / persistence (owned EXCLUSIVELY by 102034).",
      "horizontalOwned tables (owned by the horizontal module, e.g. Payment).",
      "Pages / screens / menu, per-page contract authoring, frontend client functions (statusFrontend / agentChangeFrontend).",
      "Authoring ontology / operations / workflows / rules (consumed only)."
    ]
  },
  "conventions": {
    "selfContained": "Everything lives in l2/agentChangeBackend/. The only cross-folder reuse is the L1 materialization in l2/agentMaterializeSolution/.",
    "agentNaming": "Root + focused agents: agentCb* (clean single-purpose prompts). Materialization agents are REUSED by their existing names (agentPrepareDefsL1, agentMaterializeL1Def, agentMaterializeGen).",
    "toolCallJsonSchema": "Every LLM step returns its result through ONE tool call validated against a strict JSON schema (agentNewSolution2/ns2Schemas.ts pattern + the existing submitL1Pipeline tool). collab-llm enforces the schema; cbExtract re-validates before saving. No free-form prose results.",
    "inputLayer": "Reads l4 (BUSINESS, frozen by Stage 1): l4/{module}/module.defs.ts (ontology index + relationships), l4/{module}/ontology/*.defs.ts (canonical entity kind/fields), l4/operations/*.defs.ts, l4/workflows/*.defs.ts, l4/rules/*.defs.ts. The page bffCommands + per-page contract (l2/{module}/web/contracts/{page}.defs.ts) are CONSUMED when present (produced by agentChangeFrontend). Writes l1/{module} (3 hexagonal layers).",
    "hexagonalLayers": "layer_1_external/adapters (http: routes + controllers = BFF; persistence: repositories + TableDefinition; queues/webhooks/cron/plugins) ; layer_2_application (usecases/services/ports/dto/commands/queries) ; layer_3_domain (entities/value-objects/domain-services/rules/events). Dependency points INWARD; ctx.data ONLY inside adapters/persistence.",
    "aggregateDerivation": "From ontology kind + relationships: core -> 1 physical table + domain aggregate root + repository port + repository adapter ; supporting (oneToMany/oneToOne under a core, no direct query) -> EMBEDDED in the root details JSONB as a value-object (NO own table) ; event (append-only) -> own table ; mdm -> NO table (read-only via 102034 shared MDM runtime).",
    "jsonbModel": "Maximize JSONB. Real columns ONLY for fields that need an index (PK, queried FKs, status/lifecycle, ordering/filter timestamps used by operations/workflows). Everything else + supporting child collections -> single details JSONB column (e.g. Order.details.items: OrderItem[]). Promote a details field to a column only when it becomes filtered/indexed.",
    "mdmPersistence": "MDM table persistence is EXCLUSIVELY 102034 (mdm_documents, mdm_documents_entities_index, ...). This flow NEVER emits an MDM TableDefinition and NEVER registers an MDM table in the manifest; it only reads via the shared MDM runtime. horizontalOwned follows the same principle.",
    "namingDeterminism": "All code identifiers derive deterministically from the canonical ontology entityId / operationId (Order -> domain Order, table order/orders, port OrderRepository, usecase createOrder). The PT title ('Pedido') is a UI label ONLY — never a file/class/column name. (Fixes the legacy Order -> pedidoEntity drift.)",
    "ownerStatus": {
      "allowedToProcess": [
        "toCreate"
      ],
      "ignoredInV1": [
        "toUpdate",
        "toRemove",
        "inProgress",
        "done"
      ],
      "lockStatus": "inProgress",
      "successStatus": "done",
      "failureStatus": "leave as inProgress and write a trace record; v1 has no failed enum",
      "independentOf": "statusFrontend (agentChangeFrontend) — no mandatory order between the two reconcilers"
    },
    "layerKnowledge": {
      "problem": "The BFF must know the application (layer_2) functions; the application must know the domain; and persistence must serve the domain — but the DOMAIN must not know persistence. How is the chain wired?",
      "compileTime": "Resolved by the materialization dependsFiles (.d.ts) chain, generated INNER-FIRST: domain (imports nothing) -> repository port + table (import domain) -> usecase (imports domain + port) -> repository adapter (imports port + domain + table) -> http controller (imports the usecase function/types + the per-page contract Output). Each artifact's pipeline lists EXACTLY the inner-layer .d.ts it imports, so agentMaterializeGen always has the callee's signatures as context.",
      "runtime": "Dependency Inversion. The usecase depends ONLY on the repository PORT interface (layer_2_application/ports), NEVER on the concrete adapter. register-backend builds the composition root that binds each port -> its adapter; usecases resolve repositories from ctx by port (same registry pattern as ctx.data.moduleData.getTable today). So persistence depends on the domain/port — not the reverse.",
      "correction": "There is NO domain->persistence dependency. The arrow is INVERTED: persistence (adapter) implements a port owned by the application, and imports the domain; the domain is the innermost ring and imports nothing. BFF->application and application->domain are normal inward imports; application<->persistence is inverted through the port."
    },
    "defsSelfSufficiency": "Each generated .defs.ts MUST be SUFFICIENT to generate its .ts: agentMaterializeGen (a separate agent) consumes ONLY the .defs.ts, nothing else. So every .defs.ts carries (a) the complete `data` planning block (the WHAT) and (b) `export const pipeline` = one item with everything the LLM needs: outputPath (.ts), defPath, dependsFiles[] (.d.ts signatures of the inner callee layer), dependsOn[], skills[] = the LLM CONTEXT (layer skill _102021_/l2/skills/layer_*.md + platform _102034_.d.ts), rulesPath?+rulesApplied? (authoritative rule text), afterSaveBackEnd? (deterministic register hook), agent='agentMaterializeGen'. If the LLM would need anything not in `data` nor reachable via dependsFiles/skills/rulesPath, the .defs.ts is incomplete. Canonical example: _102043_/l1/cafeFlow/layer_1_external/dailySalesMetrics.defs.ts.",
    "materialization": "Reuses the L1 pipeline: gen-* writes the `data` block; agentPrepareDefsL1 appends `export const pipeline` to each .defs.ts in dependency order; agentMaterializeL1Def resolves outputPath + dependsFiles + attaches skills/rulesPath per item via tool call; agentMaterializeGen generates the .ts from the self-sufficient .defs.ts. Then deterministic registrars wire the manifest, the router and the composition root.",
    "fanOutReads": "Parallel fan-out children are pre-allocated, reused and DELETED after completion. Consumers that run AFTER a fan-out read the SAVED .defs.ts files (file-fallback), never task payloads.",
    "verification": "Deterministic, NON-blocking: index agents check id resolution against canonical l4 ids (unresolved -> warnings); the final validate reads the SAVED l1 files (ctx.data only in adapters/persistence; no MDM table; contract shape honored).",
    "modelPolicy": "No LLM for deterministic scan, status mutation, registration and validation. When an LLM is required, force strict JSON-schema tool output. Alias by task: codeawsfast (cheap/fast) for light steps; codehigh (strong reasoning, low cost) for planning (aggregates, JSONB/index, usecase orchestration, domain); codepro (mid) for structured defs + pipeline resolution; codeinstruct (priciest, best code) RESERVED for the final .ts generation (agentMaterializeGen)."
  },
  "modelAliases": {
    "_note": "Test aliases. Stored here so the flow is self-describing; each LLM step picks one via its `modelAlias` field. modelType hints inside prompts (e.g. <!-- modelType: codepro -->) must match the chosen alias.",
    "codeinstruct": {
      "provider": "azureai",
      "model": "gpt-5.2-codex",
      "costAsProvided": {
        "input": "$1.75",
        "output": "$14.00"
      },
      "recommendedFor": [
        "final .ts code generation (agentMaterializeGen)",
        "highest-risk code where correctness matters most"
      ]
    },
    "codepro": {
      "provider": "azureai",
      "model": "Kimi-K2.6-1",
      "costAsProvided": {
        "input": "$0.95",
        "output": "$4.00"
      },
      "recommendedFor": [
        "schema-first defs generation (ports, adapters, controllers)",
        "pipeline resolution"
      ]
    },
    "codeawsfast": {
      "provider": "bedrock",
      "model": "amazon.nova-pro-v1:0",
      "costAsProvided": {
        "input": "$0.63",
        "output": "$5.00"
      },
      "recommendedFor": [
        "low-cost summaries",
        "simple classification",
        "mechanical/non-critical steps"
      ]
    },
    "codehigh": {
      "provider": "openrouter",
      "model": "z-ai/glm-5.2",
      "costAsProvided": {
        "input": "$1",
        "output": "$3"
      },
      "recommendedFor": [
        "reasoning-heavy planning: aggregate boundaries, JSONB/index decisions",
        "usecase orchestration",
        "pure domain entities"
      ]
    }
  },
  "reads": [
    "l5/project.json",
    "l5/{module}/process.defs.ts",
    "l4/{module}/module.defs.ts",
    "l4/{module}/ontology/*.defs.ts",
    "l4/operations/*.defs.ts",
    "l4/workflows/*.defs.ts",
    "l4/rules/*.defs.ts",
    "l2/{module}/web/contracts/{page}.defs.ts (per-page contract, when frontend already ran)",
    "l4/{module}/trace/behavior-health-report.json"
  ],
  "writes": [
    "l1/{module}/layer_3_domain/{entities,value-objects,domain-services,rules,events}/*.defs.ts (+ .ts)",
    "l1/{module}/layer_2_application/{usecases,services,ports,dto,commands,queries}/*.defs.ts (+ .ts)",
    "l1/{module}/layer_1_external/adapters/persistence/*.defs.ts (+ .ts) + persistence.ts manifest",
    "l1/{module}/layer_1_external/adapters/http/{controllers/*.defs.ts (+ .ts), routes/router.ts}",
    "l0/config.json (backend router + persistence entrypoints)",
    "statusBackend on processed l4 operation/workflow owners",
    "l4/trace/cb-*.json"
  ],
  "explicitlyNotWritten": [
    "MDM tables / TableDefinition (owned by 102034)",
    "horizontalOwned tables (owned by the horizontal module)",
    "Pages / screens / menu / per-page contract / frontend client functions (statusFrontend)",
    "ontology / operations / workflows / rules (consumed, never authored)"
  ],
  "steps": [
    {
      "planId": "(root)",
      "agentName": "agentChangeBackend",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [],
      "initialStatus": "waiting_human_input",
      "goal": "Root orchestrator. No input args. Reads the CLI command (run|rebuild-defs|rebuild-all) and enqueues cb-scan. Finishes immediately if the scan finds no toCreate work."
    },
    {
      "planId": "cb-scan",
      "agentName": "agentCbScanCreateOwners",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "(root)"
      ],
      "goal": "DETERMINISTIC. Read l4/l5 and select owners (operations/workflows) with statusBackend = toCreate. Enqueue cb-validate-readiness.",
      "ifEmpty": "Complete with no file/status writes."
    },
    {
      "planId": "cb-validate-readiness",
      "agentName": "agentCbValidateL4Readiness",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-scan"
      ],
      "goal": "DETERMINISTIC preflight: entity/operation refs resolve to canonical l4 ids; non-blocking warnings. Enqueue cb-lock."
    },
    {
      "planId": "cb-lock",
      "agentName": "agentCbLockOwners",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-validate-readiness"
      ],
      "goal": "DETERMINISTIC. Set statusBackend = inProgress for the selected owners (operations AND workflows). Enqueue cb-aggregate-index."
    },
    {
      "planId": "cb-aggregate-index",
      "agentName": "agentCbAggregateIndex",
      "type": "agent",
      "modelAlias": "codehigh",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-lock"
      ],
      "strictTool": true,
      "goal": "LLM. Group ontology entities into aggregates from kind + relationships (core->root, supporting->embedded, event->own append-only table classified by eventPolicy, mdm->no table). Enqueue cb-persistence-index."
    },
    {
      "planId": "cb-persistence-index",
      "agentName": "agentCbPersistenceIndex",
      "type": "agent",
      "modelAlias": "codehigh",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-aggregate-index"
      ],
      "strictTool": true,
      "goal": "LLM. Per aggregate/event table pick indexed columns vs details JSONB; event tables index owner FK + timestamp and carry retention. Enqueue cb-usecase-index."
    },
    {
      "planId": "cb-usecase-index",
      "agentName": "agentCbUsecaseIndex",
      "type": "agent",
      "modelAlias": "codepro",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-persistence-index"
      ],
      "strictTool": true,
      "goal": "LLM. Map each OPERATION owner to a usecase + its ports (workflows are orchestration, not owners). Enqueue cb-bff-index."
    },
    {
      "planId": "cb-bff-index",
      "agentName": "agentCbBffIndex",
      "type": "agent",
      "modelAlias": "codepro",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-usecase-index"
      ],
      "skipWhen": "no page contract exists yet (frontend not run) -> skips the LLM",
      "strictTool": true,
      "goal": "LLM (conditional). Index page contracts for Output refinement when present. Enqueue cb-gen-domain."
    },
    {
      "planId": "cb-gen-domain",
      "agentName": "agentCbDomainEntity",
      "type": "agent",
      "modelAlias": "codehigh",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-bff-index"
      ],
      "strictTool": true,
      "goal": "LLM, WHOLE LAYER in one call (array result). Pure domain entities per aggregate root + value-objects, plus one immutable record per persisted event. Enqueue cb-gen-port.",
      "savedArtifact": [
        "l1/{module}/layer_3_domain/entities/*.defs.ts"
      ]
    },
    {
      "planId": "cb-gen-port",
      "agentName": "agentCbRepositoryPort",
      "type": "agent",
      "modelAlias": "codepro",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-gen-domain"
      ],
      "strictTool": true,
      "goal": "LLM, whole layer. Repository ports I{Entity}Repository per aggregate; append-only ports (append+finders) per persisted event. Enqueue cb-gen-table.",
      "savedArtifact": [
        "l1/{module}/layer_2_application/ports/*.defs.ts"
      ]
    },
    {
      "planId": "cb-gen-table",
      "agentName": "agentCbPersistenceTable",
      "type": "agent",
      "modelAlias": "codehigh",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-gen-port"
      ],
      "strictTool": true,
      "goal": "LLM, whole layer. TableDefinition per core root + per persisted event (append-only, purpose 'controle', retentionDays). MDM/reaction produce NO table. Enqueue cb-gen-adapter.",
      "savedArtifact": [
        "l1/{module}/layer_1_external/adapters/persistence/{table}.defs.ts"
      ]
    },
    {
      "planId": "cb-gen-adapter",
      "agentName": "agentCbRepositoryAdapter",
      "type": "agent",
      "modelAlias": "codepro",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-gen-table"
      ],
      "strictTool": true,
      "goal": "LLM, whole layer. Repository adapters implementing the ports (domain<->row + details JSONB; MDM via 102034; event adapters do insert-only). Enqueue cb-gen-usecase.",
      "savedArtifact": [
        "l1/{module}/layer_1_external/adapters/persistence/{entity}RepositoryAdapter.defs.ts"
      ]
    },
    {
      "planId": "cb-gen-usecase",
      "agentName": "agentCbUsecase",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-gen-adapter"
      ],
      "goal": "DISPATCHER (no LLM). Fan out one usecase worker per OPERATION owner (cb-usecase-fanout) and enqueue the controller join (cb-gen-http, waiting_dependency on the fan-out)."
    },
    {
      "planId": "cb-usecase-fanout",
      "agentName": "agentCbUsecase",
      "type": "agent",
      "modelAlias": "codehigh",
      "executionMode": "parallel_dynamic",
      "dependsOn": [
        "cb-gen-usecase"
      ],
      "maxParallel": 10,
      "strictTool": true,
      "goal": "LLM WORKER per operation owner: one usecase file with explicit functions[] input/output, orchestrating domain + ports, appending its eventWrites in the same transaction. Progress: 'Gerar usecases {{completed}}/{{total}}, falhas {{failed}}'.",
      "savedArtifact": [
        "l1/{module}/layer_2_application/usecases/{usecase}.defs.ts"
      ]
    },
    {
      "planId": "cb-gen-http",
      "agentName": "agentCbHttpController",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-usecase-fanout"
      ],
      "goal": "DETERMINISTIC (no LLM). One controller per OPERATION owner (workflows generate none). Binds each handler to the usecase's REAL exported functions (read from the generated defs) — coherence by construction. Enqueue cb-materialize.",
      "savedArtifact": [
        "l1/{module}/layer_1_external/adapters/http/controllers/{owner}.defs.ts"
      ]
    },
    {
      "planId": "cb-materialize",
      "agentName": "agentCbMaterialize",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-gen-http"
      ],
      "goal": "DISPATCHER (no LLM), ONE LAYER PER DISPATCH. Spawns only the innermost stale layer (cb-mat-L{rank}) and a sequential continue-dispatcher (cb-mat-after-L{rank}) that waits on that layer's planId and re-runs this dispatch. This is a REAL barrier: the runtime's addParallelArgs forces a parallel parent to in_progress at creation, so creating all layers at once is NOT a barrier (bug: controllers materialized while usecases were 13/18). dispatch is idempotent (materialized .ts stop being stale)."
    },
    {
      "planId": "cb-mat-L{rank}",
      "agentName": "agentCbMaterialize",
      "type": "agent",
      "modelAlias": "codeinstruct",
      "executionMode": "parallel_dynamic",
      "dependsOn": [
        "cb-materialize (spawned one rank at a time; each next layer waits on the previous via cb-mat-after-L{rank})"
      ],
      "maxParallel": 10,
      "goal": "LLM WORKER per .defs.ts of the current layer: generate the final .ts from the self-sufficient defs + dependsFiles (.d.ts) + skill. Layers in order: entidades de domínio(0) -> ports(1) -> tabelas(2) -> adapters(3) -> usecases(4) -> controllers(5). Progress: 'Materializar {layer} {{completed}}/{{total}}, falhas {{failed}}'.",
      "savedArtifact": [
        "l1/{module}/**/*.ts"
      ]
    },
    {
      "planId": "cb-register",
      "agentName": "agentCbRegister",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "the last cb-mat layer"
      ],
      "goal": "DETERMINISTIC (no LLM). Write the module's backend block into the CLIENT-owned l5/project.json (backendControllers dir + persistence.tableDefsDir + routeKeys) so runtime assembly composes config.json without the masters importing the client (dependency inversion). Guarded: config failure never aborts. Enqueue cb-validate-all.",
      "savedArtifact": [
        "l5/project.json (modules[].backend)"
      ]
    },
    {
      "planId": "cb-validate-all",
      "agentName": "agentCbValidateAll",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-register"
      ],
      "goal": "DETERMINISTIC barrier. Integrity from the SAVED l1 files: (a) every usecase port has a port + domain entity defs; (b) every controller handler.usecaseRef exists as a usecase export (coherence, catches the orderFlow break); (c) COMPLETENESS: every .defs.ts has its materialized .ts (fails if materialization is incomplete — the project-level check the per-file Monaco compile misses). FAILS the run on any missing ref, so finalize cannot mark owners done prematurely. Enqueue cb-finalize when clean.",
      "savedArtifact": "l4/trace/cb-health-report.json"
    },
    {
      "planId": "cb-finalize",
      "agentName": "agentCbFinalizeStatus",
      "type": "agent",
      "modelAlias": null,
      "executionMode": "sequential",
      "dependsOn": [
        "cb-validate-all"
      ],
      "goal": "DETERMINISTIC. Set statusBackend = done for the processed owners (operations AND workflows). Runs only after cb-validate-all passes, so materialization is proven complete. Enqueue cb-final-summary."
    },
    {
      "planId": "cb-final-summary",
      "agentName": "agentCbFinalSummary",
      "type": "agent",
      "modelAlias": "codeawsfast",
      "executionMode": "sequential",
      "dependsOn": [
        "cb-finalize"
      ],
      "goal": "Append the backend run to l5/{module}/process.defs.ts, emit a concise report, clear traces, clean the task, complete."
    }
  ],
  "failurePolicy": {
    "beforeLock": "Fail without changing l4 owner statuses.",
    "afterLock": "Do not mark done. Keep affected owners in inProgress and write trace details (no failed enum in v1).",
    "partialFailure": "A failed artifact blocks done only for the owners that depend on it; unrelated owners may finish.",
    "noWork": "No selected owners means no status or file writes.",
    "manualRecovery": "A later recovery tool or v2 may reset stale inProgress owners to toCreate after inspection."
  },
  "handoffArtifacts": {
    "consumes": [
      "l4/{module}/module.defs.ts (ontology index + relationships)",
      "l4/{module}/ontology/*.defs.ts",
      "l4/operations/*.defs.ts + l4/workflows/*.defs.ts (owners; statusBackend)",
      "l4/rules/*.defs.ts",
      "l2/{module}/web/contracts/{page}.defs.ts (per-page contract, when present)"
    ],
    "produces": [
      "l1/{module}/layer_3_domain/* (entities, value-objects, domain-services, rules, events)",
      "l1/{module}/layer_2_application/* (usecases, services, ports, dto, commands, queries)",
      "l1/{module}/layer_1_external/adapters/persistence/* (TableDefinition of core/event tables; repository adapters; manifest)",
      "l1/{module}/layer_1_external/adapters/http/* (BFF controllers + routes; only when a contract exists)",
      "materialized .ts + composition root (port->adapter)",
      "statusBackend = done on the processed owners"
    ],
    "explicitlyNotProduced": [
      "MDM tables / TableDefinition (102034)",
      "horizontalOwned tables",
      "pages / screens / menu / contract authoring / frontend client functions (statusFrontend)",
      "ontology / operations / workflows / rules (consumed)"
    ],
    "consumedBy": [
      "A later L1 deploy/runtime registration",
      "future agentChangeBackend v2 for update/remove"
    ],
    "independentOf": "agentChangeFrontend (statusFrontend) — no mandatory order between the two reconcilers."
  }
}
